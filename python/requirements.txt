fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.9
pandas==2.2.1
polars==0.20.15
numpy==1.26.4
statsmodels==0.14.1
scikit-learn==1.4.2
scipy==1.13.0
Pillow==10.3.0
pyarrow==15.0.2
optuna==3.6.1

# Neon Postgres (core/database.py)
asyncpg==0.29.0
python-dotenv==1.0.1

# Excel ingestion (.xlsx / .xls) via pandas.read_excel
openpyxl==3.1.2
xlrd==2.0.1

# Neon Object Storage (S3-compatible)
boto3==1.34.84
pydantic-settings==2.2.1
# Makes Python use the OS certificate store. Without it, a TLS-inspecting
# corporate proxy breaks every call to Neon Object Storage with
# CERTIFICATE_VERIFY_FAILED. main.py calls inject_into_ssl() at startup.
truststore==0.10.4
